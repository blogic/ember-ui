<ActionSplash
  @icon="router"
  class="text-primary"
/>
{{#if (eq this.uconfig.logout_state 'idle')}}
<SettingsDescription>
  {{format-message "Disconnected due to user inactivity"}}
</SettingsDescription>
{{/if}}
<ButtonFooter>
  <BsButton @onClick={{this.onReconnect}} @type="primary">
    {{format-message "Reconnect"}}
  </BsButton>
</ButtonFooter>