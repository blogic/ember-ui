<Header />
{{#if (eq this.type "main")}}
  <SettingsHeader
    @icon="qr-code"
    @title={{format-message "WiFi"}}
    @description={{format-message
      "Scan the QR code on your mobile device to gain access to your network"
    }}
  />
{{else}}
  <SettingsHeader
    @icon="qr-code"
    @title={{format-message "Guest WiFi"}}
    @description={{format-message
      "Ask your guest to scan the QR code to gain access to the guest network"
    }}
  />
{{/if}}
{{#if (eq this.guest.model.iface.disable "false")}}
  <ButtonSelect
    @value={{this.type}}
    @value1="main"
    @option1={{format-message "Main"}}
    @value2="guest"
    @option2={{format-message "Guest"}}
    class="mt-3"
  />
{{/if}}
{{#if (eq this.type "main")}}
  <WifiQrCode
    @ssid={{this.network.model.wifi.ssid}}
    @psk={{this.network.model.wifi.key}}
  />
{{else}}
  <WifiQrCode
    @ssid={{this.guest.model.ssid.ssid}}
    @psk={{this.guest.model.ssid.key}}
  />
{{/if}}