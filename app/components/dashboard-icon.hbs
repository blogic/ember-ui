{{! template-lint-disable no-args-paths }}
<BsIcon
  @name={{@name}}
  @onClick={{this.onClick}}
  class="{{if
      (eq this.uconfig.dashboard this.args.index)
      'text-primary'
      'text-secondary'
    }}
    h1"
/>