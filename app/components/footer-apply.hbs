{{! template-lint-disable no-bare-strings }}
{{#if (eq this.uconfig.status "pending")}}
  <ButtonFooter class="mb-2">
    <BsButton @onClick={{this.commit}} @type="primary">{{format-message
        "Pending changes"
      }}</BsButton>
  </ButtonFooter>
{{/if}}